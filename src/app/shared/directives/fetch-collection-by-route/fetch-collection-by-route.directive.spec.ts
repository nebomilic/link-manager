import { FetchCollectionByRouteDirective } from './fetch-collection-by-route.directive';

describe('FetchCollectionByRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new FetchCollectionByRouteDirective();
    expect(directive).toBeTruthy();
  });
});
